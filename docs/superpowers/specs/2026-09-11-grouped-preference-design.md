# Two-group preference steering

Approved scope: upper/lower and wide/narrow, aligned with Streaming Flow Policy anchors; no P/M/C toy expansion. Continue the existing B2 worktree and frozen checkpoint.

For a complete 64-action path, d = mean(tanh(y_t / .35)), e = mean(tanh(y_t / .35)^2), excluding the initial state. Group features are f_D=((1+d)/2,(1-d)/2) and f_W=(e,1-e). Names denote continuous desirability, not a guarantee of the four midpoint classes. A prematurely aborted path has all four desirabilities zero; its failure is retained in the Monte Carlo average and receives a separate task cost. Completed paths missing the goal still have their geometric features and receive the task cost.

Within-group weights w_D,w_W and between-group importance alpha are two-element simplexes. U = sum_r alpha_r dot(w_r,f_r). Scoped direction/width comparisons omit alpha; overall comparisons use U. Fixed rho=16 for synthetic BT feedback and beta=16 for inference are explicit pilot settings in these bounded units. Learn three free coordinates, using convex staged fitting followed by bounded joint refinement; report the numerical convergence and local-identifiability diagnostics. Overall-only data cannot identify all three parameters.

Query scope pattern per ten judgments: direction,width,overall,direction,width,overall,direction,width,overall,overall. Thus K=20 comprises 6 direction, 6 width, 8 overall judgments. Queries are independent analytic paths, never derived from hidden user weights. Eight synthetic profiles cross four mode preferences with direction-priority (.8,.2) and width-priority (.2,.8); within-group desired feature weights are (.9,.1). Fit nested K=5,10,20,40; evaluate closed-loop steering at K=20 initially. Labels are synthetic, not human feedback.

For each actual replan, draw M=8 current latents. Execute each candidate's current eight-step chunk once in the evaluator and hold that prefix fixed across L=4 independent base-policy future continuations. Score expected full-option features over L before exponentiation. Each current candidate has its own independent future random draws. A failing current chunk is ineligible. A failing later continuation stays in its candidate's expectation. Soft logits are beta*expected_utility - mean((goal_error/.1)^2 + 25*task_failure); this task-conditioned target is stated separately from pure preference tilting. All-invalid guided decisions execute a hold-position chunk and record fallback. Raw base uses one current latent without preference or task selection. No future rollout is executed on the real environment beyond the selected current chunk. Execution uses actual Gym and retains failures.

Anchor: upstream SFPS sets a0 to the latest normalized observed position and integrates the joint (a,z) state. The existing checkpoint's 16 prediction points include this anchor; generating eight future commands therefore asks for nine points at local t=0..8/15 and executes points 1..8. The ODE anchor is fixed in normalized coordinates for every latent at a replan; toy instantaneous tracking makes the latest observed position equal to the last accepted command. Do not adopt a different SI policy's carry/time conventions or change checkpoint horizons. Make the anchor/command separation explicit in evaluation results and tests.

Pilot artifacts must include all queries/scopes/labels, fitted simplexes, held-out scope metrics, latent seeds, conditional-rollout diagnostics, actual trajectories, mode counts, task success, fallback/ESS, full-batch timing, and frozen-policy hashes. Compare learned and oracle using the same soft selector and candidate streams. Include base and task-only. Preserve prior flat pilot and its artifacts. Report limited proposal coverage and finite-L bias honestly. Generate GIFs from actual saved inference paths, never generated illustrations.

Normalization detail: upstream initializes the action coordinate from normalized observation coordinates, then unnormalizes all predicted points with action statistics. The canonical toy x minima differ by 7.45058e-5, so the discarded model anchor is not bitwise equal to the physical observation. Preserve this frozen-checkpoint convention, and distinguish observed anchors from raw model-anchor positions in recorded diagnostics. This is not an extra executed command.
